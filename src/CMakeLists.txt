SET(CMAKE_C_FLAGS "-Wall -O2 -s")

ADD_DEFINITIONS(-D_FILE_OFFSET_BITS=64)

SET(SRCS romdirfs.c)

ADD_EXECUTABLE(romdirfs ${SRCS})
TARGET_LINK_LIBRARIES(romdirfs fuse)

INSTALL(TARGETS romdirfs DESTINATION bin)
